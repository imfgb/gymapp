"""Body metrics — Phase 1.

Planned entity:

    UserMetricSnapshot  (owner, measured_at, weight_kg, body_fat_pct optional, notes)

Phase 2 adds `MonthlyGoal`. Height is on Profile (not snapshot-tracked).
Intentionally empty in Phase 0.
"""
